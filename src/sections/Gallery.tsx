import Section from '@/components/base/Section/Section';

const Gallery: React.FC = () => {
  return (
    <Section id="gallery">
      <div>Gallery</div>
    </Section>
  );
};

export default Gallery;
