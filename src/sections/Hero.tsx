import Section from '@/components/base/Section/Section';

const Hero: React.FC = () => {
  return (
    <Section id="hero">
      <div className="pt-[105px]">Hero</div>
    </Section>
  );
};

export default Hero;
