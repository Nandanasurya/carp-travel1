import Section from '@/components/base/Section/Section';

const Services: React.FC = () => {
  return (
    <Section id="services">
      <div>Services</div>
    </Section>
  );
};

export default Services;
