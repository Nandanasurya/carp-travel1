import Section from '@/components/base/Section/Section';

const Contacts: React.FC = () => {
  return (
    <Section id="contacts">
      <div>Contacts</div>
    </Section>
  );
};

export default Contacts;
